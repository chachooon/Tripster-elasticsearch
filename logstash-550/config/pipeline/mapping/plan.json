{
  "template": "plan-*",
  "settings": {
      "number_of_shards": 1,
      "number_of_replicas": 0,
      "analysis": {
          "analyzer": {
              "default": {
                  "type": "standard"
              },
              "ko_analyzer": {
                  "type": "custom",
                  "tokenizer": "arirang_tokenizer",
                  "analyzer": "arirang_analyzer"
              }
          }
      }
  },
  "mappings": {
    "plan": {
      "properties": {
        "Plan_ID": { "type": "integer" },
        "Member_ID": { "type": "integer" },
        "Plan_Title": { "type": "text" },
        "Plan_StartDate": { "type": "date", "format": "yyyy-MM-dd"},
        "Plan_EndDate": { "type": "date", "format": "yyyy-MM-dd"},
        "Plan_EndChk": { "type": "integer" },
        "Created": { "type": "date", "format": "yyyy-MM-dd HH:mm:ss"},
        "Updated": { "type": "date", "format": "yyyy-MM-dd HH:mm:ss"}
      }
    }
  }
}
